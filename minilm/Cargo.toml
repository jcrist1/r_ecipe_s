[package]
name = "minilm"
version = "0.1.0"
edition = "2024"

[dependencies]
candle-core = { version = "0.9.1", features = ["accelerate"]}
candle-nn = { version = "0.9.1", features = ["accelerate"]}
candle-transformers = { version = "0.9.1", features = ["accelerate"]}
safetensors = "0.5.3"
serde = "1.0.219"
serde_json = "1.0.140"
thiserror = "2.0.12"
tokenizers = { version = "0.21.1", features = ["unstable_wasm", "rustls-tls"] }
