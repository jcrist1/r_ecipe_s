version: "3.9"

services:
  postgres:
    image: custom-pg:latest
    ports: 
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: secret123
      POSTGRES_USER: r_ecipe_s_user
      POSTGRES_DB: r_ecipe_s
      PGDATA: /data/
    volumes:
      - pg_data:/data

volumes:
  pg_data: 
